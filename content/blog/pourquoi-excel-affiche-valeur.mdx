---
title: "Pourquoi Excel Affiche #VALEUR! : Solutions"
date: "2024-07-18"
author: "Thomas L'Exceleur"
excerpt: "Comprenez et resolvez l'erreur #VALEUR! qui apparait dans vos formules Excel."
description: "Guide pour corriger l'erreur #VALEUR! dans Excel. Causes courantes et solutions pratiques."
category: "Gestion d'erreurs"
catégories: ["Gestion d'erreurs", "Formules Excel"]
tags: ["Erreur VALEUR", "Depannage", "Erreurs", "Formules"]
---

# Erreur #VALEUR! dans Excel : Solutions

L'erreur #VALEUR! indique que quelque chose ne va pas avec les types de données dans votre formule.

## Signification

#VALEUR! signifie qu'Excel à trouve un type de données inattendu.

## Causes courantes

### Cause 1 : Opération sûr du texte

```excel
="abc" + 5    ' Erreur #VALEUR!
```

On ne peut pas additionner du texte et un nombre.

### Solution

Vérifiez que vos cellules contiennent bien des nombres.

### Cause 2 : Espace dans une cellule "vide"

### Symptome

La cellule semble vide mais contient un espace.

### Detection

```excel
=NBCAR(A1)
```

Retourne 1 où plus si la cellule contient des espaces.

### Solution

```excel
=SUPPRESPACE(A1)
```

### Cause 3 : Nombres stockes comme texte

### Symptome

Les nombres sont alignes à gauche.

### Detection

```excel
=TYPE(A1)
```

Retourne 2 si c'est du texte.

### Solutions

```excel
=CNUM(A1)
=A1*1
=A1+0
```

### Cause 4 : Arguments de fonction incorrects

```excel
=RECHERCHEV(A1; B1; 2; FAUX)    ' B1 doit etre une plage
```

### Solution

Vérifiez que chaque argument à le bon type.

### Cause 5 : Plages de tailles différentes

```excel
=A1:A5 + B1:B3    ' Erreur car tailles différentes
```

### Solution

Utilisez des plages de meme taille.

### Cause 6 : Caracteres invisibles

### Detection

Des caracteres non imprimables peuvent causer des erreurs.

### Solution

```excel
=EPURAGE(A1)
```

### Cause 7 : Dates en texte

```excel
=A1 - B1    ' Erreur si les dates sont en texte
```

### Solution

```excel
=DATEVAL(A1) - DATEVAL(B1)
```

## Gérer l'erreur

### SIERREUR

```excel
=SIERREUR(formule; "Erreur de valeur")
=SIERREUR(formule; 0)
=SIERREUR(formule; "")
```

### Vérification prealable

```excel
=SI(ESTNUM(A1); A1+B1; "Vérifier A1")
```

## Diagnostic

### Étape par étape

1. Identifiez quelle cellule cause l'erreur
2. Vérifiez le contenu reel (barre de formule)
3. Vérifiez le type avec TYPE()
4. Nettoyez les données

### Formule de test

```excel
=SI(ESTERREUR(formule); "Erreur"; formule)
```

## Exemples de correction

### Addition avec texte potentiel

**Avant** :
```excel
=A1 + B1
```

**Après** :
```excel
=CNUM(A1) + CNUM(B1)
```

### RECHERCHEV protegee

**Avant** :
```excel
=RECHERCHEV(A1; B:D; 2; FAUX)
```

**Après** :
```excel
=SIERREUR(RECHERCHEV(SUPPRESPACE(A1); B:D; 2; FAUX); "")
```

## Cas particuliers

### Formules matricielles

Les anciennes formules matricielles peuvent générer #VALEUR! si mal validees.

**Solution** : Validez avec Ctrl + Maj + Entrée (anciennes versions).

### Fonctions de date

```excel
=JOUR("abc")    ' Erreur #VALEUR!
```

**Solution** : Vérifiez que la cellule contient une vraie date.

## Prevention

### Validation des données

1. **Données** > **Validation des données**
2. Limitez les saisies aux types corrects

### Mise en forme conditionnelle

Colorez les cellules qui pourraient causer des erreurs.

### Vérification automatique

```excel
=SI(ESTNUM(A1); "OK"; "Vérifier")
```

## Tableau recapitulatif

| Cause | Detection | Solution |
|-------|-----------|----------|
| Texte au lieu de nombre | TYPE()=2 | CNUM() |
| Espaces | NBCAR()>0 | SUPPRESPACE() |
| Caracteres invisibles | Visuel | EPURAGE() |
| Mauvais arguments | Relire formule | Corriger |

## Bonnes pratiques

1. **Nettoyez** les données importees
2. **Vérifiez** les types avant calcul
3. **Utilisez** SIERREUR pour gérer les erreurs
4. **Validez** les saisies utilisateur

[Découvrir nos formations Excel](/formations-excel)

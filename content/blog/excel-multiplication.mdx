---
title: "Excel Multiplication : Toutes les Methodes"
date: "2024-07-18"
author: "Thomas L'Exceleur"
excerpt: "Decouvrez toutes les facons de faire des multiplications dans Excel, de la plus simple a la plus avancee."
description: "Guide complet sur la multiplication dans Excel. Operateur *, fonction PRODUIT, multiplication matricielle et astuces pratiques."
category: "Formules Excel"
categories: ["Formules Excel", "Calculs"]
tags: ["Multiplication", "Formules", "Calculs", "PRODUIT"]
---

# Excel Multiplication : Toutes les Methodes

La multiplication est l'une des operations les plus courantes dans Excel. Voici toutes les facons de la realiser.

## Methode 1 : L'operateur *

La methode la plus simple :

```excel
=A1*B1
```

### Exemples

```excel
=5*3           ' Resultat: 15
=A1*10         ' Multiplie A1 par 10
=A1*B1*C1      ' Multiplie trois cellules
```

## Methode 2 : La fonction PRODUIT

Pour multiplier plusieurs valeurs :

```excel
=PRODUIT(nombre1; nombre2; ...)
```

### Exemples

```excel
=PRODUIT(A1:A10)       ' Multiplie toutes les valeurs de A1 a A10
=PRODUIT(A1;B1;C1)     ' Multiplie trois cellules
=PRODUIT(2;3;4;5)      ' Resultat: 120
```

### Avantage sur l'operateur *

PRODUIT ignore les cellules vides et les textes, evitant les erreurs.

## Multiplier une colonne par un nombre fixe

### Avec references absolues

Pour multiplier toute une colonne par un taux fixe (ex: TVA 20%) :

```excel
=A1*$B$1
```

Le signe **$** fige la reference lors de la copie.

## Multiplier des plages

### Methode SOMMEPROD

Pour multiplier element par element et sommer :

```excel
=SOMMEPROD(A1:A10;B1:B10)
```

Equivalent a : `A1*B1 + A2*B2 + ... + A10*B10`

## Multiplication matricielle

### Fonction PRODUITMAT

Pour les calculs matriciels (algebre lineaire) :

```excel
=PRODUITMAT(matrice1;matrice2)
```

**Note** : Le nombre de colonnes de matrice1 doit egaler le nombre de lignes de matrice2.

## Cas pratiques

### Calcul de TVA

```excel
=Prix_HT * 1,20
```

### Prix total

```excel
=Quantite * Prix_unitaire
```

### Conversion de devises

```excel
=Montant_EUR * Taux_change
```

### Pourcentage

```excel
=Valeur * 15%    ' Calcule 15% de la valeur
```

## Erreurs courantes

### #VALEUR!

**Cause** : Multiplication avec du texte

**Solution** : Verifiez que toutes les cellules contiennent des nombres

### Resultat 0 inattendu

**Cause** : Cellule vide (traitee comme 0)

**Solution** : Utilisez SI pour gerer les cas vides

```excel
=SI(B1="";"";A1*B1)
```

## Collage special : Multiplier des valeurs existantes

Pour multiplier des valeurs en place :

1. Entrez le multiplicateur dans une cellule (ex: 1,1)
2. Copiez cette cellule (**Ctrl + C**)
3. Selectionnez les cellules a modifier
4. **Collage special** (**Ctrl + Alt + V**)
5. Cochez **Multiplication** > OK

## Tableau recapitulatif

| Methode | Usage | Exemple |
|---------|-------|---------|
| * | Multiplication simple | =A1*B1 |
| PRODUIT | Plusieurs valeurs | =PRODUIT(A1:A10) |
| SOMMEPROD | Multiplier et sommer | =SOMMEPROD(A1:A10;B1:B10) |
| PRODUITMAT | Matrices | =PRODUITMAT(A1:B2;C1:D2) |

[Decouvrir la formation Excel Avance](/formations-excel/excel-avance)

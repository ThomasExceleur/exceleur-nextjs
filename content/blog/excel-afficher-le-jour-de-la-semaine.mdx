---
title: "Excel Afficher le Jour de la Semaine"
date: "2024-07-18"
author: "Thomas L'Exceleur"
excerpt: "Apprenez à afficher le jour de la semaine à partir d'une date dans Excel."
description: "Guide pour afficher le jour de la semaine dans Excel. Fonctions JOURSEM, TEXTE et formats personnalises."
category: "Dates"
catégories: ["Dates", "Formules Excel"]
tags: ["Jour", "Date", "Semaine", "Format"]
---

# Afficher le Jour de la Semaine dans Excel

Plusieurs méthodes permettent d'obtenir le jour de la semaine à partir d'une date.

## Méthode 1 : Format personnalise

### Afficher le jour complet

1. Sélectionnez la cellule avec la date
2. **Ctrl + 1** (Format de cellule)
3. **Personnalisee**
4. Tapez : `jjjj`

### Formats disponibles

| Format | Résultat |
|--------|----------|
| `j` | Jour (1-31) |
| `jj` | Jour sûr 2 chiffres (01-31) |
| `jjj` | Jour abrege (lun, mar...) |
| `jjjj` | Jour complet (lundi, mardi...) |

### Combiner avec la date

```
jjjj jj/mm/aaaa
```

Affiche : lundi 15/01/2024

## Méthode 2 : Fonction TEXTE

### Syntaxe

```excel
=TEXTE(date; "jjjj")
```

### Exemples

```excel
=TEXTE(A1; "jjjj")      ' lundi
=TEXTE(A1; "jjj")       ' lun
=TEXTE(AUJOURDHUI(); "jjjj")  ' Jour actuel
```

### Avantage

Le résultat est du texte utilisable dans des concatenations.

```excel
="Nous sommes "&TEXTE(AUJOURDHUI();"jjjj")
```

## Méthode 3 : Fonction JOURSEM

### Syntaxe

```excel
=JOURSEM(date; [type])
```

### Types de retour

| Type | Dimanche | Lundi |
|------|----------|-------|
| 1 (defaut) | 1 | 2 |
| 2 | 7 | 1 |
| 3 | 6 | 0 |

### Exemples

```excel
=JOURSEM(A1)       ' 1=Dim, 2=Lun... 7=Sam
=JOURSEM(A1; 2)    ' 1=Lun, 2=Mar... 7=Dim
```

## Convertir le numéro en nom

### Avec CHOISIR

```excel
=CHOISIR(JOURSEM(A1;2);"Lundi";"Mardi";"Mercredi";"Jeudi";"Vendredi";"Samedi";"Dimanche")
```

### Avec INDEX

```excel
=INDEX({"Lundi";"Mardi";"Mercredi";"Jeudi";"Vendredi";"Samedi";"Dimanche"};JOURSEM(A1;2))
```

## Cas pratiques

### Identifier les week-ends

```excel
=SI(OU(JOURSEM(A1)=1;JOURSEM(A1)=7);"Weekend";"Semaine")
```

### Identifier les jours ouvres

```excel
=SI(ET(JOURSEM(A1;2)>=1;JOURSEM(A1;2)<=5);"Ouvre";"Ferme")
```

### Mise en forme conditionnelle

Colorez les week-ends en gris :
1. Sélectionnez la plage de dates
2. **Mise en forme conditionnelle** > **Nouvelle règle**
3. Formule : `=OU(JOURSEM(A1)=1;JOURSEM(A1)=7)`
4. Format : fond gris

## Premier jour de la semaine

### Trouver le lundi de la semaine

```excel
=A1-JOURSEM(A1;2)+1
```

### Trouver le dimanche de la semaine

```excel
=A1-JOURSEM(A1;2)+7
```

## Numéro de semaine

### Fonction NO.SEMAINE

```excel
=NO.SEMAINE(A1)
```

### Norme ISO

```excel
=NO.SEMAINE.ISO(A1)
```

## Langues

### Français par defaut

Le jour s'affiché dans la langue de votre Excel.

### Forcer une langue

Modifiez les paramètrès régionaux de Windows.

## Comparaison des méthodes

| Méthode | Résultat | Type |
|---------|----------|------|
| Format jjjj | lundi | Affichage |
| TEXTE | "lundi" | Texte |
| JOURSEM | 2 | Nombre |

## Erreurs courantes

### Date en texte

Si la date est stockee comme texte, convertissez-la d'abord :

```excel
=TEXTE(DATEVAL(A1);"jjjj")
```

### Mauvais type JOURSEM

Vérifiez le paramèêtre type selon vos besoins.

## Bonnes pratiques

1. **Format** pour l'affichage simple
2. **TEXTE** pour les concatenations
3. **JOURSEM** pour les calculs et conditions

[Découvrir nos formations Excel](/formations-excel)
